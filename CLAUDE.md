# CLAUDE.md

The full instructions for this repository live in **`AGENTS.md`**, next to this file, and are
imported here so they load in full:

@AGENTS.md

This file is a pointer rather than a copy so the two can never drift, and a **real file rather than a
symlink** so it survives being unzipped on a machine that doesn't support them.
